export default {
  theme: {
    extend: {
      fontFamily: {
        mono: ['"JetBrains Mono"', 'monospace'],
        sans: ['"JetBrains Mono"', 'sans-serif'], // Set as default font across all text
      },
    },
  },
};
